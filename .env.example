# Frontend (Vite) — variables de build.
# En producción (Vercel) apunta a la URL pública del backend, incluyendo /api.
# En desarrollo NO hace falta: se usa el proxy de Vite hacia http://localhost:4000.
VITE_API_URL=https://mantelek-api.up.railway.app/api
